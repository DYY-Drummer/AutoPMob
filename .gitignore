# API キー・秘密情報（絶対にコミットしない）
.env
.env.*
.env.local
*.pem
*_secret*

# Phase 5 sweep 個別ログ・中間 JSON（_summary.json はコミット可）
experiments/phase5_sweep/**/*.log
experiments/phase5_sweep/**/epochs=*

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# 仮想環境
.venv/
venv/
ENV/

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# 出力（必要に応じてコミットする場合はコメントアウト）
extracted_equations.json
extracted_equations_normalized.json
unified_equations.json
*.pdf
!docs/*.pdf

# 再生成可能な大容量グラフ（build_graph_data.py で再構築、GitHub 100MB 超）
*.pt
equation_graph.pt

# 生 PDF（研究データ、容量大・著作権物のためコミットしない）
input_pdfs/*
!input_pdfs/.gitkeep
pdfs/

# エディタ・スクリプトのバックアップ（データ *.bak.json は対象外）
*.py.bak
*.sh.bak

# Claude Code 内部状態（worktree 等）
.claude/

# LaTeX 中間ファイル（docs/）
docs/*.aux
docs/*.log
docs/*.toc
docs/*.out
docs/*.dvi
docs/*.synctex.gz
docs/*.fls
docs/*.fdb_latexmk

# ---- 2026-09-07 リポジトリ整理: 名前指定ステージ運用を成文化 ----

# Cursor IDE のエージェント定義（自動生成 156 件・本研究リポジトリでは未使用）
.cursor/

# 実験の実行ログ・完了マーカー・スモーク退避（正典 JSON のみコミットする運用）
experiments/*.log
experiments/*.done
experiments/*_smoke.json

# シード別の生データ（サイズ大・スクリプトで再生成可能。統計 JSON のみコミット）
experiments/xd/
experiments/xd_cc/
experiments/xd_dof/
experiments/xg/
experiments/xg_A/
experiments/xs1/
experiments/xs1_k200/

# ゼミテンプレ配下の生成物（図は Overleaf zip で配布・従来どおり非追跡）と
# docs 下位ディレクトリの LaTeX 中間物（従来の docs/*.aux 系をサブディレクトリへ拡張）
docs/ゼミlatexテンプレ/figure/
docs/ゼミlatexテンプレ/build2/
docs/**/*.aux
docs/**/*.log
docs/**/*.toc
docs/**/*.out
docs/**/*.dvi
docs/**/*.fls
docs/**/*.fdb_latexmk
docs/**/*.synctex.gz

# 先輩の修論 TeX ソース（参照用ローカルコピー・他者の著作物のため非コミット）
thesis/TexSource/

# 旧 DB のバックアップスナップショット（unified_equations.json 本体と同じ扱いで非コミット）
unified_equations.bak.json

# OS
.DS_Store
Thumbs.db
